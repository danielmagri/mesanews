import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:mesa_news/app/modules/login/login_module.dart';

void main() {
  initModule(LoginModule());
  // EmailController email;
  //
  setUp(() {
    //     email = LoginModule.to.get<EmailController>();
  });

  group('EmailController Test', () {
    //   test("First Test", () {
    //     expect(email, isInstanceOf<EmailController>());
    //   });

    //   test("Set Value", () {
    //     expect(email.value, equals(0));
    //     email.increment();
    //     expect(email.value, equals(1));
    //   });
  });
}
