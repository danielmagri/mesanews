import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('EmailPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EmailPage(title: 'Email')));
    //  final titleFinder = find.text('Email');
    //  expect(titleFinder, findsOneWidget);
  });
}
